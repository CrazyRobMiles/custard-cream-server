// Shared by pages/pictures.js (camera uploads) and pages/manage.js (AI edit
// results) - kept in one place so the two never drift apart.
module.exports = {
    'image/jpeg': '.jpg',
    'image/png': '.png'
};
